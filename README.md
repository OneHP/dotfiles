# dotfiles

```bash
git clone https://github.com/OneHP/dotfiles.git
ln -s dotfiles/.gitconfig .gitconfig
ln -s dotfiles/.mavenrc .mavenrc
git clone https://github.com/OneHP/vim.git
ln -s vim .vim
ln -s dotfiles/.vimrc .vimrc
ln -s dotfiles/.zshrc .zshrc
rm .zshrc
ln -s dotfiles/.zshrc .zshrc

```
